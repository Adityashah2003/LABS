DATA SEGMENT
A DB 44H,11H,22H,55H
B DB ?
COUNT DW 0004H
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START:
MOV AX,DATA
MOV DS,AX

MOV BX, COUNT
DEC BX

OUTLOOP:
MOV CX,BX
MOV SI,0

INLOOP:
MOV AL,A[SI]
INC SI
CMP AL,A[SI]
JB GO
XCHG AL,A[SI]
MOV A[SI-1],AL
GO:
LOOP INLOOP
DEC BX
JNZ OUTLOOP

MOV AH, 4CH
INT 21H
CODE ENDS
END START