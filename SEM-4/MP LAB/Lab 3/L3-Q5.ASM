DATA SEGMENT
NUM1 DD 55555555H
NUM2 DD 11111111H

DIFF DD 00H
BORROW DW 00H
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE,DS:DATA

START:
	MOV AX, DATA
	MOV DS, AX

	MOV CX,02H
	MOV SI,0
	CLC;
UP:
	MOV AX,NUM1[SI]
	SBB AX,NUM2[SI]
	MOV DIFF[SI],AX

	INC SI
	INC SI	
	LOOP UP

	ADC BORROW,0;

MOV AH, 4CH 
INT 21H
CODE ENDS
END START