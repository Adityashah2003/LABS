DATA SEGMENT
A DT 98765432109876543210H
B DT 11111111111111111111H
ANS DT 0
BORROW DB 0
DATA ENDS

CODE SEGMENT
ASSUME CS: CODE, DS: DATA
START:  MOV AX, DATA
        MOV DS, AX

        MOV CX, 5
        CLC
        MOV SI, 0
NEXT:   MOV AX, WORD PTR A[SI]
        SBB AX, WORD PTR B[SI]
        MOV WORD PTR ANS[SI], AX
        INC SI
        INC SI
        LOOP NEXT

        ADC BORROW, 0

        MOV AH, 4CH
        INT 21H
CODE ENDS
END START
