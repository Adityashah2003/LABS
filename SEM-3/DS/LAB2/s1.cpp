#include <iostream>
#include <string>
#include<stdio.h>
using namespace std;

int main() {
    char str[100],str1[100],str2[100];
    int i,j=0;
    gets(str);
    for(i=0; str[i]!='\0'; i++){ }
        cout << "the length of the string is " << i;
return 0;
}
