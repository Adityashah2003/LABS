DATA SEGMENT
A DD 88888888H
B DD 12345678H
ANS DD 0
BORROW DB 0
DATA ENDS

CODE SEGMENT
ASSUME CS: CODE, DS: DATA
START:  MOV AX, DATA
        MOV DS, AX

        MOV CX, 2
        MOV SI, 0
        CLC
BACK:   MOV AX, WORD PTR A[SI]
        SBB AX, WORD PTR B[SI]
        MOV WORD PTR ANS[SI], AX
        INC SI
        INC SI
        LOOP BACK

        ADC BORROW, 0

        MOV AH, 4CH
        INT 21H
CODE ENDS
END START
